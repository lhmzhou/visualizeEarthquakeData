define(function(require) {
  var d3 = require('ds');
  var topojson = require('topojson');
  var E-data Visual = require('datamaps');
  console.log('woah');
console.log(d3, topojson);
    // Begin drawing for SVG map
   var map = new Datamap({element: document.getElementById('container')});

});
